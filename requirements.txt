# Phantom Lyrics - Dependencies
# Install with: pip install -r requirements.txt

# GUI Framework for the transparent overlay window
PySide6>=6.5.0

# Windows API bindings for click-through and window management (Windows only;
# on Linux/macOS the overlay uses Qt's cross-platform flags instead)
pywin32>=306; sys_platform == "win32"

# WebSocket server (asynchronous)
websockets>=12.0

# HTTP requests for LRCLib API
requests>=2.31.0

# Global hotkey listener for gaming-mode toggle (click-through lock)
pynput>=1.7.6
